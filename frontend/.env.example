# Browser-facing API base (same origin via Next rewrite)
NEXT_PUBLIC_API_URL=/api

# Where Next proxies /api → Express (server-side only, not exposed to browser)
BACKEND_INTERNAL_URL=http://127.0.0.1:5013
